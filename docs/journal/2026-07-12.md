# Journal de travail — 2026-07-12

Récapitulatif des changements apportés au dépôt `maasga-code` aujourd'hui.
Tous les commits ci-dessous sont sur `main` et poussés sur GitHub
(`origin/main` = `9adbc46`).

## Vue d'ensemble

| Commit | Type | Résumé |
|--------|------|--------|
| `3eb30e9` | chore | Réorganisation de la racine + nettoyage des fichiers parasites/morts |
| `6d0aa81` | feat | Fiches produit dédiées `/catalogue/:id` + morph View Transitions |
| `a7ef1b2` | fix | Suppression de l'effet cercle autour du curseur |
| `07b02ce` | build | Recompilation de `tailwind.css` après la suppression du curseur |
| `9adbc46` | chore | Retrait de l'app mobile Flutter du dépôt web |

État production : déployé sur Cloudflare Pages (`maasga-website.pages.dev`).

---

## 1. Réorganisation & nettoyage de la racine (`3eb30e9`)

Le dossier racine accumulait des fichiers de debug, des doublons et des
médias non rangés. Actions :

- **Documentation** → déplacée dans `docs/` : `ARCHITECTURE_MAASGA.md`,
  `DEPLOYMENT_CHECKLIST.md`, `GUIDE_ADMIN.md`, `HANDOFF.md`,
  `MAASGA_Plan_Integration_Unifie.md`, `QUICK_START.md`,
  `README_FINAL.md`, `SETUP_FINAL.md`. (`README.md` reste à la racine.)
- **Scripts** → déplacés dans `scripts/` : `generate_images.ps1`,
  `build_css.ps1`.
- **Handoff design** : `Website improvement suggestions.zip` extrait dans
  `docs/design_handoff_homepage_redesign/`, le zip supprimé.
- **Médias de marque orphelins** → `docs/brand-assets/` : `one.png`,
  les deux maquettes de logo `hf_*`/`*cover_sd*.jpeg`, `HOMZ3.mp4`,
  `mann.mp4`.
- **Image cassée corrigée** : `ines.jpeg` déplacée dans `public/` (elle
  était référencée dans `a-propos.tsx` mais absente → image cassée en
  prod, maintenant réparée).
- **Doublons supprimés** : copies racine identiques à `public/`
  (`cherif.jpeg`, `malick.jpg`, `techman.png`, `logo-site.png`,
  `logo site.png`).
- **Fichiers de debug/morts supprimés** : dumps HTTP (`body*.txt`,
  `headers*.txt`, `response*.html`, `verify*.html`, `espace_*`), scripts
  jetables `fix_admin_encoding*.py`, doublon `ARCHITECTURE_MAASGA.txt`.
- **Cache de build** : `.wrangler/tmp` (~187 Mo, régénérable) nettoyé.
- **`.gitignore`** enrichi pour ignorer ces catégories à l'avenir.
- **`secrets_tmp.json`** : conservé (déjà gitignoré, non tracké) — c'est
  un doublon du secret officiel de `.dev.vars`.

> Non touché volontairement : le dossier `src/` et le worktree
> `.worktrees/catalogue-product-pages` (branche divergente, laissée
> intacte à la demande).

## 2. Fiches produit dédiées + View Transitions (`6d0aa81`)

Fonctionnalité majeure (travail qui était non commité, désormais figé) :

- **Nouvelle route** `GET /catalogue/:id` (`src/index.tsx`) rendant une
  **fiche produit dédiée** (`src/pages/produit.tsx`).
- **Morph d'image** entre la carte du catalogue et la fiche produit via
  l'API **View Transitions** (`view-transition-name: product-image-{id}`
  partagé), déclenché par le routeur PJAX de `Layout.tsx`. Repli propre
  sur Safari/Firefox.
- **Composant partagé** `src/components/CheckoutModals.tsx` : panier +
  commande + paiement extraits de `catalogue.tsx` (qui passe de 1844 à
  613 lignes) et mutualisés avec la fiche produit.
- **Libellés techniques** extraits dans `src/data/techSpecsLabels.ts`.
- **Effet tilt** des cartes revu (carte entière en 3D, suivi réactif) et
  **nouvelle section accueil « Notre Expertise »** (scroll épinglé
  split-screen) dans `home.tsx` + `app.css`.
- Ajustements associés sur `a-propos`, `avis`, `contact`,
  `realisations`, `rendez-vous`, `simulateur`. `CLAUDE.md` mis à jour.

Audité avant commit (revue correction/sécurité/XSS ciblée sur le JS
panier/checkout) puis déployé.

## 3. Suppression du curseur personnalisé (`a7ef1b2` + `07b02ce`)

- Retrait de l'effet **cercle qui suivait le curseur** (`#cursor-dot` :
  le `<div>`, son script de suivi GSAP dans `Layout.tsx`, et son CSS dans
  `app.css`). Le site utilise à nouveau le curseur natif.
- `tailwind.css` recompilé pour que le bundle committé corresponde à la
  prod.
- `tsc --noEmit` : OK. Déployé et vérifié (plus aucune trace de
  `cursor-dot` dans le HTML de production).

## 4. Retrait de l'app mobile Flutter (`9adbc46`)

- Le dossier `mobile/` (app Flutter Android V1 : code, docs, plans) est
  **retiré du dépôt web** — c'était un projet séparé qui n'a pas sa place
  dans la codebase web.
- **Conservé en local** : une copie de travail du source `mobile/` reste
  sur la machine, désormais **gitignorée** (`mobile/` ajouté au
  `.gitignore`) pour ne pas être re-trackée.
- Le source reste récupérable depuis l'historique git au besoin.
- Perdus (non récupérables, mais régénérables via Flutter) : les
  artefacts non-trackés `build/`, `.dart_tool/`, `.idea/`.

---

## Notes / points ouverts

- **Worktree `catalogue-product-pages`** : branche
  `feat/catalogue-product-pages-view-transitions` (2 commits non
  fusionnés, dernier le 2026-07-04). Vraisemblablement **obsolète** — la
  fonctionnalité a été réimplémentée en plus complet dans `6d0aa81`. À
  supprimer/archiver quand tu veux (laissée intacte pour l'instant).
- **Fichiers `mobile/app/...` auto-générés** qui apparaissaient dans
  `git status` : réglés par le retrait du dossier mobile.
- Le projet **ODAA** (ERP local-first) est un dépôt totalement séparé
  (`Maasga/odaa`, cloné dans `C:\Users\sayta\Projects\odaa`) — aucun lien
  physique avec `maasga-code`.
